<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Site Collection</title>
    <link rel="stylesheet" href="/css/main.css">
</head>
<body>
    <%- include('partials/navbar', { page: '/sites' }) %>
    <main>
        <h1 class="text-4xl font-bold">
            Site Collection 
            <span class="text-base text-primary">(<%= sites.length %>)</span>
          </h1>
          
        <table>
            <tr>
                <th>Image</th>
                <th>Site Name</th>
                <th>Date</th>
                <th>Location</th>
                <th>Region</th>
                <th>Details</th>
            </tr>
            <% sites.forEach(site => { %>
                <tr>
                    <td><img src="<%= site.image %>" width="100"></td>
                    <td><%= site.name %></td>
                    <td>Date: <%= site.date %> (<%= site.dateType %>) <br> Designated: <%= site.designated %></td>
                    <td>
                        <%= site.location %>, 
                        <a href="/sites?provinceOrTerritory=<%= site.ProvinceOrTerritory.name %>">
                            <button><%= site.ProvinceOrTerritory.name %></button>
                        </a>
                    </td>
                    <td>
                        <a href="/sites?region=<%= site.ProvinceOrTerritory.region %>">
                            <button><%= site.ProvinceOrTerritory.region %></button>
                        </a>
                    </td>
                    <td>
                        <a href="/sites/<%= site.siteId %>">
                            <button>Details</button>
                        </a>
                    </td>
                </tr>
            <% }) %>
        </table>
    </main>
</body>
</html>
